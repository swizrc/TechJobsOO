@model TechJobs.Models.Job

@{
// DONEZO-TODO #2.1- declare the ViewModel (outside of this @{ } block)
}

<table class="job-listing">
    <tr>
        <th>Name</th>
        <td>@Model.Name</td>
    </tr>
    <tr>
        <th>Emplotyer</th>
        <td>@Model.Employer.ToString()</td>
    </tr>
    <tr>
        <th>Location</th>
        <td>@Model.Location.ToString()</td>
    </tr>
    <tr>
        <th>Core Competency</th>
        <td>@Model.CoreCompetency.ToString()</td>
    </tr>
    <tr>
        <th>Position Type</th>
        <td>@Model.PositionType.ToString()</td>
    </tr>
    @{ 
            // DONEZO-TODO #2.2 - display the job fields, one per row (outside of this @{ } block)
    }
</table>